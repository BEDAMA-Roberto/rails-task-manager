<h1>Nuestras tareas</h1>
<% @tasks.each do |task| %>
  <%# <h2><%= task.title<%= task.details</h2> %> %> %>
  <h2><%= tlink_to "task.title", new_task_path(task) %><%= task.details %></h2>
  <small>task.completed</small>
  <%# <%= link_to "mas detalles", new_task_path(task) %> %>
<% end %>